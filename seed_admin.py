"""
RETIRED — this script has been replaced by seed_users.py.

seed_users.py seeds three accounts (admin, teacher, student) and is
idempotent (safe to re-run). Use it instead:

    python seed_users.py
"""

if __name__ == "__main__":
    print("This script has been retired. Run: python seed_users.py")
